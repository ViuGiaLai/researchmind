# ================================================================
# ResearchMind Cloudflare Pages — Wrangler Configuration
# ================================================================
# The name must match your Cloudflare Pages project name.
# Compatibility flags ensure the functions runtime works correctly.
# ================================================================

name = "researchmind"
compatibility_date = "2026-07-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./docs"

[build]
command = "npm install"
