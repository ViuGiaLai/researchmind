# Dev placeholder — API keys are set in backend/.env
# CI overwrites this file with real secrets from ${{ secrets.DOTENV }}
EMBEDDING_MODE=cloud
EMBEDDING_MODEL=gemini-embedding-001
EMBEDDING_DIM=768
ENABLE_RERANKER=False
GEMINI_API_KEY=
LLM_MODE=cloud_free
HOST=127.0.0.1
PORT=8765
